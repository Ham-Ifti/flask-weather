pipeline {
    agent any
    
    stages {
        stage("Build Docker Image") {
            steps {
                sh 'docker'
                sh 'echo hello world please work'
            }
        }
    }
}